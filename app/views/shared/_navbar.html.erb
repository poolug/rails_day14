<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <%= link_to 'Home', root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <% if  user_signed_in? %>
                <li class="nav-item active p-3">
                    <%= link_to current_user.name, profile_path(current_user.id) %>
                </li>
                <li class="nav-item active p-3">
                    <%= link_to 'Cerrar Sesión', destroy_user_session_path, method: :delete, class:"btn btn-danger" %>
                </li>
            <% else %>
                <li class="nav-item active p-3">
                    <%= link_to 'Sign In', new_user_session_path %>
                </li>
                <li class="nav-item sctive p-3">
                    <%= link_to 'Cerrar Sesión', new_user_registration_path %>
                </li>
            <% end %>

        </ul>
    </div>
</nav>